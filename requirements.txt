# ==============================================================================
# Relay Controller Studio - Python dependencies
# ==============================================================================
# Install with:
#   pip install -r requirements.txt
#
# GUI framework
PySide6>=6.6.0

# ------------------------------------------------------------------------
# The following are NOT required by Phase 1 (no detection/upload logic is
# implemented yet), but are listed here as commented-out placeholders for
# the phase that adds real board detection and flashing, so future setup
# only requires uncommenting rather than re-researching dependencies:
#
# pyserial>=3.5          # COM port enumeration for board detection
# esptool>=4.7            # ESP32 flashing
#   (Arduino UNO/Nano compilation + upload will use the external
#    `arduino-cli` binary, invoked as a subprocess -- not a pip package)

# ------------------------------------------------------------------------
# Packaging (only needed to build the Windows .exe, not to run from source)
# pyinstaller>=6.3.0
